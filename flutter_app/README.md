# flutter_app

Flutter scaffold converted from the React project. See `kal_app_flutter` at workspace root for original scaffold.

To run (requires Flutter SDK):

```bash
cd kal_app/flutter_app
flutter pub get
flutter run
```
